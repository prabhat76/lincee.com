# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-morning-credit-ahu0h3sm-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_8HDt2LTngcGa}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true
spring.jpa.generate-ddl=true
spring.jpa.defer-datasource-initialization=false

# Server Configuration
server.port=${PORT:8080}
# server.servlet.context-path=/api/v1

# Multipart Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Pagination Configuration
spring.data.web.pageable.default-page-size=100
spring.data.web.pageable.max-page-size=1000

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:4200,https://linceecom-production.up.railway.app,https://lincee-ui.vercel.app
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH,HEAD
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# Security Configuration
app.jwt.secret=linceeSecretKeyForJWTTokenGenerationAndValidation2024
app.jwt.expiration=86400000

# Stripe Payment Gateway Configuration
stripe.api.key=${STRIPE_SECRET_KEY:sk_test_your_stripe_secret_key_here}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_your_webhook_secret_here}
stripe.currency=usd
stripe.success.url=${STRIPE_SUCCESS_URL:http://localhost:3000/payment/success}
stripe.cancel.url=${STRIPE_CANCEL_URL:http://localhost:3000/payment/cancel}

# Cloudinary Image Storage Configuration
cloudinary.cloud-name=dt6pfj9bb
cloudinary.api-key=862764889694815
cloudinary.api-secret=qT2uTT3L-svC1situfoL2xGcEec

# Logging Configuration
logging.level.com.lincee=debug
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.springframework.boot.autoconfigure.orm.jpa=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics

# Application Configuration
app.name=Lincee Streetwear Backend
app.version=1.0.0
app.description=Complete E-commerce Backend Solution

# Springdoc OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.doc-expansion=none
springdoc.swagger-ui.show-extensions=true
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.swagger-ui.filter=true
springdoc.packages-to-scan=com.lincee.controller
springdoc.paths-to-match=/api/v1/**
management.endpoint.health.show-details=when-authorized